import 'package:flutter/material.dart';

class DetailTherapistPage extends StatelessWidget {
  const DetailTherapistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Therapist'),
      ),
      body: Column(
        children: [Text('data')],
      ),
    );
  }
}
